import os


def directorio (ruta):
        os.chdir(ruta)


def crear_carpetas():
        os.makedirs("Docuemntos", exist_ok = true)
        os.makedirs("Imagenes", exist_ok = true)
        os.makedirs("Software", exist_ok = true)
        os.makedirs("Otros", exist_ok = true)

def ordenar(doc, image, soft, otros):
        for i in os.listdir():
                i = i.lower(
            if 

        else:
            if:
            elif:
            else.
                )
        